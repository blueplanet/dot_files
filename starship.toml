# 不用在提示符前换行
add_newline = false

# 当 starship 扫描当前目录下的文件和文件夹时，最多使用 10 毫秒
scan_timeout = 10


[cmd_duration]
min_time = 500

[directory]
truncation_length = 3

[git_branch]
symbol = "🌱 "
truncation_length = 100
truncation_symbol = "..."

[hostname]
ssh_only = true
disabled = false

